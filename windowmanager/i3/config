# vim: filetype=i3
# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4
set $alt Mod1
set $term terminal

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:Monoid NF 11

bar {
  separator_symbol " "
  status_command i3blocks
  position top
  mode dock
  modifier None
  strip_workspace_numbers yes
  colors {
    background $bg-color
    statusline $text-color
  }
  # tray_output none
}

# thin borders
hide_edge_borders both
for_window [class="^.*"] border pixel 1

focus_follows_mouse no

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

bindsym $mod+F12 exec $HOME/.config/i3/touchpad-toggle

# start a terminal
bindsym $mod+Return exec $term

# kill focused window
bindsym $mod+Shift+q kill

# start rofi (a program launcher)
bindsym $mod+d exec --no-startup-id rofi -modi drun -i -l 10 -show drun

# start window switcher
bindsym $mod+Tab exec --no-startup-id rofi -modi windowcd -i -show windowcd

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

bindsym $mod+Shift+period exec --no-startup-id "~/bin/dmenuunicode"

bindsym $mod+$alt+Left exec --no-startup-id "bash ~/bin/i3resize left"
bindsym $mod+$alt+Down exec --no-startup-id "bash ~/bin/i3resize down"
bindsym $mod+$alt+Up exec --no-startup-id "bash ~/bin/i3resize up"
bindsym $mod+$alt+Right exec --no-startup-id "bash ~/bin/i3resize right"

bindsym $mod+$alt+h exec --no-startup-id "bash ~/bin/i3resize left"
bindsym $mod+$alt+j exec --no-startup-id "bash ~/bin/i3resize down"
bindsym $mod+$alt+k exec --no-startup-id "bash ~/bin/i3resize up"
bindsym $mod+$alt+l exec --no-startup-id "bash ~/bin/i3resize right"
bindsym $mod+$alt+p exec --no-startup-id "/opt/enpass-beta/Enpass6 showassistant"
bindsym $mod+$alt+m exec --no-startup-id ~/bin/displayctl menu

# toggle split in vertical/horizontal orientation
bindsym $mod+v split toggle

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle
bindsym $mod+$alt+f floating enable; border none; move position 0 0; resize set 1920 1080

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+Shift+s layout stacking
bindsym $mod+Shift+t layout tabbed
bindsym $mod+Shift+e layout toggle split

bindsym $mod+comma workspace prev
bindsym $mod+period workspace next
bindsym $mod+minus workspace back_and_forth

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle; border pixel 1

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
bindsym $mod+y focus child

bindsym $mod+p exec ~/bin/1pmenu
bindsym $mod+Shift+p exec ~/bin/1pswitchvault
bindsym $mod+numbersign exec rofi-pass
bindsym $mod+s exec ~/bin/toggle_screensaver

set $ws1 "1"
set $ws2 2:Dev
set $ws3 3:Music
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10:jack"

# switch to workspace
bindsym $mod+1 workspace $ws1
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3
bindsym $mod+4 workspace $ws4
bindsym $mod+5 workspace $ws5
bindsym $mod+6 workspace $ws6
bindsym $mod+7 workspace $ws7
bindsym $mod+8 workspace $ws8
bindsym $mod+9 workspace $ws9
bindsym $mod+0 workspace $ws10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $ws1
bindsym $mod+Shift+2 move container to workspace $ws2
bindsym $mod+Shift+3 move container to workspace $ws3
bindsym $mod+Shift+4 move container to workspace $ws4
bindsym $mod+Shift+5 move container to workspace $ws5
bindsym $mod+Shift+6 move container to workspace $ws6
bindsym $mod+Shift+7 move container to workspace $ws7
bindsym $mod+Shift+8 move container to workspace $ws8
bindsym $mod+Shift+9 move container to workspace $ws9
bindsym $mod+Shift+0 move container to workspace $ws10

# reload the configuration file
bindsym $mod+Shift+c exec "i3-msg reload && notify-send -a i3 -u low 'Reloaded'"

# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart

# exit i3 (logs you out of your X session)
bindsym $mod+Shift+x exec "~/.config/i3/i3exit"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width 10 px or 10 ppt
        bindsym j resize grow height 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

set $border-color        #928374
set $bg-color            #282828
set $inactive-bg-color   #1d2021
set $text-color          #ebdbb2
set $inactive-text-color #a89984
set $urgent-bg-color     #cc241d
set $indicator-color     #d65d0e

# set window colors
#                       border             background         text                 indicator
client.focused          $bg-color      $border-color          $text-color          $indicator-color
client.unfocused        $inactive-bg-color $inactive-bg-color $inactive-text-color $indicator-color
client.focused_inactive $inactive-bg-color $inactive-bg-color $inactive-text-color $indicator-color
client.urgent           $urgent-bg-color   $urgent-bg-color   $text-color          $indicator-color

gaps inner 14
gaps outer -2

set $mode_gaps Gaps: (o) outer, (i) inner
set $mode_gaps_outer Outer Gaps: +|-|0 (local), Shift + +|-|0 (global)
set $mode_gaps_inner Inner Gaps: +|-|0 (local), Shift + +|-|0 (global)
bindsym $mod+Shift+g mode "$mode_gaps"

mode "$mode_gaps" {
	bindsym o	mode "$mode_gaps_outer"
	bindsym i	mode "$mode_gaps_inner"
	bindsym Return 	mode "default"
	bindsym Escape 	mode "default"
}

mode "$mode_gaps_inner" {
	bindsym plus	gaps inner current plus 5
	bindsym minus	gaps inner current minus 5
	bindsym 0	gaps inner current set 0

	bindsym Shift+plus	gaps inner all plus 5
	bindsym Shift+minus	gaps inner all minus 5
	bindsym Shift+0		gaps inner all set 0

	bindsym Return 	mode "default"
	bindsym Escape 	mode "default"
}

mode "$mode_gaps_outer" {
	bindsym plus	gaps outer current plus 5
	bindsym minus	gaps outer current minus 5
	bindsym 0	gaps outer current set 0

	bindsym Shift+plus	gaps outer all plus 5
	bindsym Shift+minus	gaps outer all minus 5
	bindsym Shift+0		gaps outer all set 0

	bindsym Return 	mode "default"
	bindsym Escape 	mode "default"
}

smart_gaps on
smart_borders on

# Shadertone
for_window [title="shadertone"] floating disable;

# floating windows
for_window [class="Pavucontrol"] floating enable
for_window [class="Kalu"] floating enable
for_window [class="Cadence"] floating enable
for_window [title="Minecraft Launcher"] floating enable
for_window [class="Pinentry"] sticky enable
for_window [class="qjackctl"] floating enable
for_window [class="sonic-pi" window_type=splash] floating enable

# Workspace assignments
assign [title="GIMP Startup"] $ws5
assign [class="Gimp"] $ws5
assign [class="Code"] $ws2
assign [class="Sonic Pi"] $ws3
assign [class="Sonic Pi" window_type=splash] $ws1
assign [class="qjackctl"] $ws10

# Gimp
for_window [window_role="gimp-dock"] floating disable; move left; resize shrink width 50 px or 50ppt
for_window [window_role="gimp-toolbox"] floating disable; move right; resize grow width 30 px or 30ppt

# Bindings to make the webcam float and stick
for_window [title="mpvfloat"] floating enable; sticky enable; border enable;
no_focus [title="mpvfloat"]
for_window [class="Sonic Pi" window_type=splash] floating enable, border pixel 0

# Backlight control

bindsym XF86MonBrightnessDown exec --no-startup-id ~/bin/displayctl bl down
bindsym XF86MonBrightnessUp exec --no-startup-id ~/bin/displayctl bl up

bindsym XF86Sleep exec systemctl hibernate

# Multimedia Keys

# volume
bindsym XF86AudioRaiseVolume exec --no-startup-id ~/bin/lmc up
bindsym XF86AudioLowerVolume exec --no-startup-id ~/bin/lmc down

# granular volume control
bindsym $mod+XF86AudioRaiseVolume exec --no-startup-id ~/bin/lmc fineup
bindsym $mod+XF86AudioLowerVolume exec --no-startup-id ~/bin/lmc finedown

# mute
bindsym XF86AudioMute exec --no-startup-id ~/bin/lmc mute

bindsym XF86AudioPlay exec --no-startup-id ~/bin/lmc toggle
bindsym XF86AudioPause exec --no-startup-id ~/bin/lmc pause
bindsym XF86AudioStop exec --no-startup-id ~/bin/lmc stop
bindsym XF86AudioNext exec --no-startup-id ~/bin/lmc next
bindsym XF86AudioPrev exec --no-startup-id ~/bin/lmc prev

bindsym $mod+F5 exec --no-startup-id ~/bin/lmc toggle
bindsym $mod+F6 exec --no-startup-id ~/bin/lmc stop
bindsym $mod+F7 exec --no-startup-id ~/bin/lmc prev
bindsym $mod+F8 exec --no-startup-id ~/bin/lmc next
bindsym $mod+F9 exec --no-startup-id ~/.config/mutt/etc/mailsync.sh

bindsym XF86ScreenSaver exec --no-startup-id betterlockscreen -l dimblur
bindsym $mod+Shift+$alt+k exec --no-startup-id "killall screenkey || screenkey"

# App shortcuts
bindsym $mod+w exec "/usr/bin/qutebrowser"
bindsym $mod+Shift+w exec "/usr/bin/qutebrowser"
bindsym $mod+e exec $term -e neomutt
bindsym $mod+n exec $term -e /usr/bin/ranger
bindsym $mod+Shift+n exec --no-startup-id networkmanager_dmenu
bindsym $mod+m exec $term -e /usr/bin/ncmpcpp
bindsym $mod+$alt+d exec ~/bin/getpocket
bindsym $mod+$alt+s exec "syncthing --browser-only"

# Autostart apps
exec --no-startup-id ~/bin/startjack
exec_always --no-startup-id /usr/bin/betterlockscreen -w
exec --no-startup-id dunst
exec --no-startup-id mpd
# xbanish makes the mouse invisible after a 2000 ms and while typing
exec --no-startup-id xbanish -bt 2000

# Remap Caps-Lock
exec --no-startup-id ~/bin/remaps

# Enable Screen saver
exec --no-startup-id "xset s on"
exec --no-startup-id /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1
exec --no-startup-id "compton -cC --xrender-sync-fence"
# exec --no-startup-id nm-applet
exec --no-startup-id "xss-lock --ignore-sleep -- /usr/bin/betterlockscreen -l dimblur"
exec --no-startup-id "udiskie -2ans -f ~/bin/ranger-open"
exec --no-startup-id "redshift"
exec --no-startup-id "1pserver"
exec --no-startup-id syncthing -no-browser
exec --no-startup-id "/usr/bin/transmission-daemon -o -y -b -er -c ~/Downloads -T -w ~/Downloads"
exec --no-startup-id "/usr/bin/onedrive -m"
# exec --no-startup-id "/usr/bin/nextcloud"
